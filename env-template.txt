# ReturnsX Security Environment Variables Template
# Copy this to .env and replace the placeholder values

# STEP 1: Generate secure keys at https://www.random.org/strings/
# Settings: Number of strings: 3, Length: 64, Character set: Hex (0-9,a-f)
RETURNSX_ENCRYPTION_KEY="REPLACE_WITH_64_CHAR_HEX_STRING"
RETURNSX_HASH_SALT="REPLACE_WITH_64_CHAR_HEX_STRING"
SESSION_SECRET="REPLACE_WITH_64_CHAR_HEX_STRING"

# STEP 2: Production Security Settings (keep these as-is)
SESSION_SECURE="true"
NODE_ENV="production"
DATABASE_SSL_MODE="require"

# STEP 3: Update with your actual database URL
DATABASE_URL="postgresql://username:password@host:5432/database?sslmode=require"

# STEP 4: Update with your Shopify app credentials
SHOPIFY_API_KEY="your_shopify_api_key_here"
SHOPIFY_API_SECRET="your_shopify_api_secret_here"
SHOPIFY_SCOPES="read_orders,read_customers,read_fulfillments,read_refunds"

# Optional: Logging (recommended settings)
LOG_LEVEL="info"
AUDIT_LOG_RETENTION_DAYS="2555"

# Optional: WhatsApp Integration (uncomment if needed)
# TWILIO_ACCOUNT_SID="your_twilio_account_sid"
# TWILIO_AUTH_TOKEN="your_twilio_auth_token"
# WHATSAPP_WEBHOOK_SECRET="your_webhook_secret"

# Optional: Rate Limiting (uncomment if using Redis)
# RATE_LIMIT_REDIS_URL="redis://localhost:6379"
# RATE_LIMIT_MEMORY_CLEANUP_INTERVAL="300000"
